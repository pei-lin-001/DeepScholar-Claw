# DeepScholar Contracts

这里集中存放 `DeepScholar-Claw` 的共享数据契约和服务边界描述。

目标：

- 让编排层、文献层、实验层、证据层说同一种“结构化语言”。
- 避免把关键对象类型散落在各个服务目录里。
- 为后续接入数据库、消息总线、审批流和外部 API 保留稳定接口。

当前包含：

- 项目章程与阶段定义
- 研究计划与实验规格
- 结论-证据账本结构
- 服务描述模型
