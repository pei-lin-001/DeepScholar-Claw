# DeepScholar-Claw .env 示例
#
# 用法：
# 1) 复制为 `.env`
# 2) 只填你用得到的项（不需要的就留空）
# 3) 不要把真实密钥提交到 git

# -----------------------------------------------------------------------------
# 文献检索（可选）
# -----------------------------------------------------------------------------
# Semantic Scholar API key（可选，但能提升配额/稳定性）
# DEEPSCHOLAR_SEMANTIC_SCHOLAR_API_KEY=

# OpenAlex polite pool（建议填写你的邮箱）
# DEEPSCHOLAR_OPENALEX_MAILTO=you@example.com

# -----------------------------------------------------------------------------
# Neo4j（仅当你使用 graph-backend=neo4j 时需要）
# -----------------------------------------------------------------------------
# DEEPSCHOLAR_NEO4J_URI=neo4j://127.0.0.1:7687
# DEEPSCHOLAR_NEO4J_USERNAME=neo4j
# DEEPSCHOLAR_NEO4J_PASSWORD=change-me

